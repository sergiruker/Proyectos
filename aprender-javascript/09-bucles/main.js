// Bucle For
/* 
Bucle es una estructura de control que ejecuta un bucle de código 
varias veces de forma automática mientras que se cumpla una condición 
*/

/* 
for(inicialización; condición; incremento){

    Código que se ejecutará repetidamente

}
*/

let limite = 177;

for(let contador = 0; contador <= limite; contador++){

    //Bloque de código que se ejecutará
    console.log("Vamos por el número: " + contador);

}